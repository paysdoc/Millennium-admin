# Prepare Application

Prepare the application for review by installing dependencies and starting the dev server.

## Instructions

1. Run `npm install` to install dependencies
2. Start the dev server in the background with `npm run dev`
3. Wait for the server to be ready on `http://localhost:3000`
